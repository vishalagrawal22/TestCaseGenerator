#include <bits/stdc++.h>

#define endl "\n"
#define int long long
#define double long double
#define precise \
    fixed << setprecision(12)

using namespace std;

int rnd(int a, int b)
{
    return a + rand() % (b - a + 1);
}

int32_t main()
{
    srand(time(0));
    // uncomment in case of multi testcases
    // cout << 1 << endl;
}